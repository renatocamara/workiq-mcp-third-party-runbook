# Evidence logs from validation runs contain real mailbox/calendar data. Never commit.
validation-evidence-*.log
*.log

# Token caches (workiq-mcp.sh stores its token in ~/.workiq_token, but be safe)
.workiq_token
*.token

# Local environment files
.env
.env.*

# Working documents (customer-facing docx/pptx/xlsx are internal; never publish)
*.docx
*.pptx
*.xlsx
~$*

# Personal speech/teleprompter notes (local only, never commit)
speech-*.md

# Editor and OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
